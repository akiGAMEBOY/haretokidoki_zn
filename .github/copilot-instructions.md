# Copilot instructions for this workspace

- For repository commit workflows, prefer the slash command /commit-workflow.
- Use the workflow argument to choose between development, publish-toggle, and article-batch scenarios.
- Keep publication-state changes limited to the frontmatter `published` field.
- Prefer concise, conventional commit messages that are easy to reuse across projects and article batches.
