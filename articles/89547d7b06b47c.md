---
title: "指定のZIPファイルを解凍するFunction"
emoji: "🐡"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["powershell"]
published: false
---
指定したZIPファイルを解凍。

```powershell:
Function Extract-ZipFile {
    [CmdletBinding()]
    Param(
        [Parameter(Mandatory=$true)]
        [string]$zipFilePath,

        [Parameter(Mandatory=$true)]
        [string]$destinationPath
    )

    Begin {
        # ZIPファイルが存在するか確認
        if (-not (Test-Path -Path $zipFilePath)) {
            Write-Error "指定されたZIPファイルが見つかりません: $zipFilePath"
            return
        }

        # 展開先のフォルダが存在しない場合は作成
        if (-not (Test-Path -Path $destinationPath)) {
            New-Item -Path $destinationPath -ItemType Directory
        }
    }

    Process {
        # ZIPファイルを展開
        Expand-Archive -Path $zipFilePath -DestinationPath $destinationPath
    }

    End {
        Write-Output "ZIPファイルが展開されました: $zipFilePath -> $destinationPath"
    }
}

```
